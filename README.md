This bot can uses the currently-beta version of discord Party games ,

for running the bot in repl.it :
  you import it from github
  do `pip install -r requirements.txt`
  and then you run the code! , easy enough right?
